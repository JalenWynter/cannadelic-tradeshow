# Environment variables (optional — mostly for build/dev)
NODE_ENV=development

# GitLab publish (scripts/gitlab-publish.sh) — copy to .env, never commit .env
# GITLAB_TOKEN=glpat-xxxxxxxx
# NAMESPACE_ID=10

# GitLab CI sets CI=true automatically; no secrets required for offline kiosk runtime.
