[build]
builder = "NIXPACKS"

[deploy]
startCommand = "DATA_DIR=/app/data npm start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
